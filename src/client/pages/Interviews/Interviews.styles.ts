import styled from 'styled-components';

export const Layout = styled.div`
    display: grid;

    grid-template-columns: 40% 1fr;

    column-gap: 10vw;

    min-height: 90vh;
    width: 80vw;
    margin: 0 auto;
`;
